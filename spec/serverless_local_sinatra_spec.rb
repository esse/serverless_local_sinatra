RSpec.describe ServerlessLocalSinatra do
  it "has a version number" do
    expect(ServerlessLocalSinatra::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
